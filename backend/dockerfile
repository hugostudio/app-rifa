FROM node:18-alpine

ENV SUPERTOKENS_CONNECTION_URI=${SUPERTOKENS_CONNECTION_URI}
ENV SUPERTOKENS_APP_NAME=${SUPERTOKENS_APP_NAME}
ENV SUPERTOKENS_APP_DOMAIN=${SUPERTOKENS_APP_DOMAIN}
ENV SUPERTOKENS_WEBSITE_DOMAIN=${SUPERTOKENS_WEBSITE_DOMAIN}
ENV SUPERTOKENS_APIKEY=${SUPERTOKENS_APIKEY}

 ADD . /restBase